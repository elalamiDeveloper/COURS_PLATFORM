import styled from 'styled-components';

import { ChaptersList } from '.';

const CourContentContainer = styled.aside``;

const CourContent = () => {
  return (
    <CourContentContainer>
      <h3></h3>
      <ChaptersList />
    </CourContentContainer>
  );
};

export default CourContent;
